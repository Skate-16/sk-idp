apiVersion: v1
kind: Service
metadata:
  name: oauth-api
spec:
  type: NodePort
  selector:
    app: oauth-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      nodePort: 31000
